export type FiltrosSecop = {
        nitEntidad: string,
        fechaDesde: Date,
        fechaHasta: Date,
        referencia: string,
        estado: string,
        descripcion: string,
        proveedor: string
    }