export type FiltrosSecop = {
        idEntidad: string,
        fechaDesde: Date,
        fechaHasta: Date,
        referencia: string,
        estado: string,
        descripcion: string,
        proveedor: string
    }